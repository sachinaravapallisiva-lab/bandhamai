# Copy to .env.local for local work. Never commit real secrets.
# Sai: set the same names in Vercel → Project → Settings → Environment Variables
# for Production, Preview, and Development.

# Existing
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
XAI_API_KEY=

# Stripe messaging ($9.99/mo). All four are required for checkout + webhook sync.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_PRICE_ID=

# Optional later. Checkout does not use this. Add a second Price in Stripe if you
# want a $5.99 founding plan later, then wire it in a follow-up.
# STRIPE_FOUNDING_PRICE_ID=

# VerifyAI $4.99 one-time. Separate Price from STRIPE_PRICE_ID.
STRIPE_VERIFYAI_PRICE_ID=

# Meetup event ticket. Separate one-time Price from STRIPE_PRICE_ID ($9.99/mo)
# and STRIPE_VERIFYAI_PRICE_ID. Checkout fails closed if this is empty.
# Do not invent a Price ID or a dollar amount in the app. Sai sets the live Price.
STRIPE_EVENT_PRICE_ID=

# Priority pin is $4.99 for 7 days in the UI. Checkout stays fail closed.
# Do not invent a Stripe Price ID. Do not invent STRIPE_PIN_PRICE_ID.

# VerifyAI handoff. Hosted flow and/or session API. Do not commit real values.
VERIFYAI_START_URL=
VERIFYAI_API_URL=
VERIFYAI_API_KEY=
VERIFYAI_WEBHOOK_SECRET=

# Optional. Used for Checkout success/cancel URLs if Origin is missing.
# NEXT_PUBLIC_SITE_URL=https://bandhamai.vercel.app

# Support tickets (Bandham assistant). Ticket rows still save if email fails.
RESEND_API_KEY=
SUPPORT_INBOX_EMAIL=sachin.aravapallisiva@gmail.com
# Optional. Defaults to Resend's onboarding sender until a Bandham domain is verified.
# RESEND_FROM_EMAIL=

# Phone voice support (xAI Grok Voice Agent). Fail closed if unset.
# Stub only. Set the live value in Vercel. Do not commit a real secret.
BANDHAM_VOICE_SUPPORT_SECRET=

# Support human handoff destination. Server only. Never publish on the site.
# Operator default is +14709620438. Transfer caller ID stays +18032655233.
# Do not set this to the 640 subscribe outbound number. Do not prefix NEXT_PUBLIC_.
BANDHAM_SUPPORT_HANDOFF_E164=
